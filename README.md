i have made the product app as required i have used plain 
html css js as said first i fetched the elements by id to use 
them and fetched the data from api using async await and  fetch
and stored the data into  the empty array initialized earlier
and used template llliterals using map to write cards html and render
them into ui and them used join to convert them into single string 
used filter and includes and  toggle to make the favourite functionality 
work added event listner to input to implement search functionality and 
used filter and includes to make it work 
